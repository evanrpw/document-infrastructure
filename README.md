# Shared Document Infrastructure
current ingest.py needs better handling for tables, etc (and likely would benefit from semantic chunking)
Possible TODOs:
chunk tables etc on their own
smart ocr detection
separate tables (pdfplumber)
document intelligence?
use semantic chunking w pdf stucture (so tables are one chunk)
